#include <iostream>
using namespace std;
string keyin0,keyin1,keyin2,sysimg;
int cnt = 0;
char tmp[256];
int check(keyin)
{
	if (keyin0 = "img" or keyin0 = "IMG"){
		if(keyin1 = "create" or keyin1 = "CREATE"){
			if(keyin2 = "centos" or keyin2 = "CENTOS"0){
				strftime(tmp, sizeof(tmp), "%Y-%m-%d %H:%M:%S", localtime(&timep));
				cout << "System CentOS Created"
			}
		}
	}
}
int main()
{
	system("title LinuxEmulator bash.vision alpha.1.0.0");
	cout << "welcome to LinuxEmulator bash.vision alpha.1.0.0" << endl;
	while (true)
	{
		cout << "[user//LE.bash]#";
		cin >> keyin0 >> keyin1 >> keyin2;
		check(keyin);
	}
}
